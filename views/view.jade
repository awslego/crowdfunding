doctype html
html
  head
    meta(charset='utf-8')
    title Mission Impossible
    link(rel="stylesheet" type="text/css" href="/stylesheets/main.css")
  body
    div(id='container')

      label(class='col-lg-2 control-label')
        a Users &nbsp;&nbsp; | &nbsp;&nbsp;
        a(href='/makers') Makers

      <img src="/images/mission_movie.png" width=100% height=100%>
      h2(id='description')
        span='Current Fund Amount: ' + fund_balance
        font(color='gray')  ETH

      h2(id='description')

        span='Remaining Token Balance: ' + fund_tamount
        font(color='gray')  AWX

      button(id='button' onClick="location.href='/users/new'") Join
      br
      div(style=display)
        
        font(color='red') ▶︎  CrowdFund 참여 결과
        h2(id='description')
          font(color='red')  •
          span=' 참여자 : ' + user

        h2(id='description')
          font(color='red')  •
          span=' 보유 금액 : ' + user_balance
          font(color='gray')  ETH

        h2(id='description')
          font(color='red')  •
          span=' 보유 토큰 : ' + user_tamount
          font(color='gray')  TDs

